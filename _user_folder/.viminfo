# 本 viminfo 檔案是由 Vim 8.0 所產生.
# 如果想要自行修改請特別小心！

# Viminfo version
|1,4

# 'encoding' 在此檔建立時的值
*encoding=utf-8


# hlsearch on (H) or off (h):
~H
# 命令列 歷史記錄 (新到舊):
:x

# 搜尋字串 歷史記錄 (新到舊):

# 運算式 歷史記錄 (新到舊):

# 輸入行  歷史記錄 (新到舊):

# Debug Line 歷史記錄 (新到舊):

# 暫存器:

# 檔案標記:

# Jumplist (由新到舊):

# 檔案內 Mark 記錄 (由新到舊):

